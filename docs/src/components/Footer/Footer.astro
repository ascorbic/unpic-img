---
import AvatarList from "./AvatarList.astro";
type Props = {
  path: string;
};
const { path } = Astro.props;
const currentYear = new Date().getFullYear();
---

<footer>
  <AvatarList path={path} /> © {currentYear} Matt Kane
</footer>

<style>
  footer {
    margin-top: auto;
    padding: 2rem;
    border-top: 1px solid var(--theme-divider);
  }
</style>
