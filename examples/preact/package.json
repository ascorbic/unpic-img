{
  "private": true,
  "name": "preact-demo",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "preact-cli": "next",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "@unpic/preact": "workspace:^",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2"
  }
}
